# Repo-root requirements — THIS is the file Streamlit Community Cloud installs.
# (Streamlit Cloud reads requirements.txt from the repository root, not from
# the app's subdirectory. Keep this in sync with
# bfsi-risk-intelligence-platform/requirements.txt.)
# Versions pinned to match the environment the model .pkl files were
# serialized with, so they unpickle cleanly in the cloud.

# Core Data Science Libraries
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0

# Frontend
streamlit==1.58.0

# Model Persistence
joblib==1.5.3

# Batch file uploads
openpyxl==3.1.5        # read Excel (.xlsx) files
python-docx==1.2.0     # read a table from a Word (.docx) file

# Utilities
python-dotenv==1.0.1
